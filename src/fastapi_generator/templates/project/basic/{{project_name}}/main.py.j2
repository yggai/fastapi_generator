"""
FastAPI 基础项目 - {{display_name}}
"""
from fastapi import FastAPI
import uvicorn

app = FastAPI(
    title="{{display_name}}",
    description="A FastAPI project",
    version="0.1.0"
)


@app.get("/")
def root():
    """
    根路由
    """
    return {"message": "Hello from {{display_name}}!"}


if __name__ == "__main__":
    uvicorn.run("main:app", host="0.0.0.0", port=8000, reload=True) 